apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio
spec:
  entryPoints:
    - web-local
    - websecure-local
  routes:
  - match: Host(`minio.steffenillium.de`)
    kind: Rule
    services:
    - name: console
      port: http
