apiVersion: v1
kind: Service
metadata:
  name: smb-server-service-tcp139
  namespace: paperless
  labels:
    app: smb-server-service-tcp139
spec:
  type: ClusterIP
  ports:
    - port: 139
      protocol: TCP
      targetPort: 139
  selector:
    app: smb-server
