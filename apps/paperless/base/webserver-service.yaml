apiVersion: v1
kind: Service
metadata:
  labels:
    app: paperless-webserver
  name: paperless-webserver
spec:
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: paperless-webserver
status:
  loadBalancer: {}
