apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: paperless-app-pvc
  name: paperless-app-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
status: {}
