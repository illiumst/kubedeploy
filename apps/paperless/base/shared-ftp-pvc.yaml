apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: shared-ftp-pvc
  name: shared-ftp-pvc
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
