apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emby
spec:
  storageClassName: "rook-cephfs"
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
