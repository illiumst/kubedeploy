apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sftpgo
spec:
  storageClassName: "rook-cephfs"
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
